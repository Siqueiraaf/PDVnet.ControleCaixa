<UserControl x:Class="PDVnet.ControleCaixa.UI.Views.Movimentacao.MovimentacaoList"
             xmlns:converter="clr-namespace:PDVnet.ControleCaixa.UI.Converters"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             xmlns:local="clr-namespace:PDVnet.ControleCaixa.UI.Views.Movimentacao"
             mc:Ignorable="d" 
             d:DesignHeight="450" d:DesignWidth="800">
    
    <UserControl.Resources>
        <converter:StatusConverter x:Key="StatusConverter"/>
    </UserControl.Resources>
    
    <Grid>

        <DataGrid Grid.Row="2" 
                  ItemsSource="{Binding Movimentacoes}" 
                  SelectedItem="{Binding MovimentacaoSelecionada, Mode=TwoWay}" 
                  AutoGenerateColumns="False" 
                  CanUserAddRows="False" 
                  Margin="0,10,0,10">
            <DataGrid.Columns>
                <DataGridTextColumn Header="Descrição" Binding="{Binding Descricao}" Width="*"/>
                <DataGridTextColumn Header="Tipo" Binding="{Binding Tipo}" Width="120"/>
                <DataGridTextColumn Header="Categoria" Binding="{Binding Categoria}" Width="180"/>
                <DataGridTextColumn Header="Valor" Binding="{Binding Valor}" Width="120"/>
                <DataGridTextColumn Header="Status" Binding="{Binding Status, Converter={StaticResource StatusConverter}}" Width="100"/>
                <DataGridTextColumn Header="Data" Binding="{Binding DataMovimento, StringFormat={}{0:dd/MM/yyyy HH:mm:ss}}" Width="150"/>
            </DataGrid.Columns>
        </DataGrid>

    </Grid>
</UserControl>
